# envy-cli (PyPI)

Thin installer that downloads the native `envy` Rust binary for your platform.

```bash
pip install envy-cli
envy run python main.py
```

Pin a version:

```bash
ENVY_VERSION=v0.1.0 pip install envy-cli
```
